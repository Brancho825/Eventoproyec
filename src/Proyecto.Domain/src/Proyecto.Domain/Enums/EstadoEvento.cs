namespace Proyecto.Domain.Enums
{
    // Corresponde a la especificación de Evento (borrador, publicado, cancelado)
    public enum EstadoEvento
    {
        Borrador = 1, 
        Publicado = 2, 
        Cancelado = 3 
    }
}