namespace Proyecto.Domain.Enums
{
    // Corresponde a la especificación de Reserva (pendiente, confirmada, cancelada, expirada)
    public enum EstadoReserva
    {
        Pendiente = 1, 
        Confirmada = 2, 
        Cancelada = 3, 
        Expirada = 4 
    }
}