namespace Proyecto.Domain.Enums
{
    // Corresponde a la especificación de Notificación (pendiente, enviado, fallido)
    public enum EstadoNotificacion
    {
        Pendiente = 1, 
        Enviada = 2, 
        Fallida = 3 
    }
}