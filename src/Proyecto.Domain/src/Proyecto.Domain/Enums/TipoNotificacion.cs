namespace Proyecto.Domain.Enums
{
    // Corresponde a la especificación de Notificación (email, SMS, push)
    public enum TipoNotificacion
    {
        Email = 1, 
        SMS = 2, 
        Push = 3 
    }
}